; Copy to secrets.ini (gitignored) and fill in. These become WIFI_DEFAULT_SSID /
; WIFI_DEFAULT_PASS, the credentials StaticWiFi_Conn falls back to when LittleFS
; holds none -- without them a freshly flashed board comes up SoftAP-only.
[secrets]
wifi_ssid = YourSSID
wifi_pass = YourPassword
